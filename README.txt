To create the virtual environment, use:
    python3 -m venv venv || <folder-name>

To activate the virtual environment, use:
    source ./venv/bin/activate or <folder-name>/bin/activate
If activated, the terminal starts with (<folder-name>)

To deactivate the virtual environment, use:
    deactivate
